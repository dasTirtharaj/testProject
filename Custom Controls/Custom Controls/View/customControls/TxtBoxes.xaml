<UserControl x:Class="Custom_Controls.View.customControls.TxtBoxes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:oxy="http://oxyplot.org/wpf"
             xmlns:local="clr-namespace:Custom_Controls.View.customControls"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="400">
    
    <Grid>
        <!--<TextBox x:Name="txt_Box" FontSize="20" FontStyle="Normal" Margin="5,0,0,0" VerticalAlignment="Center"  TextChanged="VisiblityChecker" Panel.ZIndex="0"/>
        <TextBlock x:Name="txt_placeholder"  VerticalAlignment="Center" FontSize="20" FontWeight="ExtraLight" Foreground="Gray"  Margin="5,0,0,0" Panel.ZIndex="-1"  />
        <Button x:Name="btn"  Content="X" Width="50" HorizontalAlignment="Right" Click="btn_Clear" />-->
        <oxy:PlotView Model="{Binding MyModel}" />
    </Grid>
</UserControl>
